// uploadMiddleware.js - Starter middleware for file uploads (Phase 2A)

export const uploadMiddleware = (req, res, next) => {
  // Starter placeholder - to be implemented in future phases
  if (next) next();
};

export default uploadMiddleware;
