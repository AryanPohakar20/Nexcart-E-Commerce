import { Router } from 'express';

export const  param($m) $m.Groups[2].Value.ToUpper() dminUserManagementRoutes = () => {
  const router = Router();
  return router;
};

export default  param($m) $m.Groups[2].Value.ToUpper() dminUserManagementRoutes;
