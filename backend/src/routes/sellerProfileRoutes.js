import { Router } from 'express';

export const  param($m) $m.Groups[2].Value.ToUpper() ellerProfileRoutes = () => {
  const router = Router();
  return router;
};

export default  param($m) $m.Groups[2].Value.ToUpper() ellerProfileRoutes;
