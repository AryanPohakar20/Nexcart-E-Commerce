{
  "name": "nexcart-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node run-dev.js",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "copy-logo": "npm run copy-logo --prefix frontend"
  }
}
